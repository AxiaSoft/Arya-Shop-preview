# 🛡️ گزارش امنیتی و بازطراحی بک‌اند — آریا شاپ

**تاریخ:** ۱۴۰۵/۰۶/۱۸ (۲۰۲۶-09-09) · **دامنه بررسی:** کل سایت (بک‌اند `Db.php`، frontend، فایل‌های سرویس‌دهی) · **وضعیت:** تمام موارد بحرانی و بالا روی سمت سرور اصلاح و با تست خودکار پوشش داده شدند.

---

## ۱) خلاصه مدیریتی

بازبینی نشان داد نسخه قبلی عملاً یک **API بدون مرز** بود: هر بازدیدکننده‌ای می‌توانست جدول کاربران را کامل بخواند (شامل هش رمزها)، روی هر جدولی بنویسد یا رکورد حذف کند؛ `config.php` قابل‌دانلود بود و CORS کاملاً آزاد (`*` + credentials) تعریف شده بود. ضمناً کل کد به MySQL قفل بود و رمز عبور کاربران در مرورگر و با `btoa` (بدونهش!) «هش» می‌شد.

در این نسخه:

| مورد | وضعیت |
|---|---|
| پشتیبانی از MySQL/MariaDB، PostgreSQL، SQLite، SQL Server (پدلا) | ✅ لایه دایالکت `includes/db_engine.php` |
| پنل‌های هاستینگ cPanel / DirectAdmin / Plesk + حالت دستی | ✅ `includes/panels.php` + ویزارد نصب |
| احراز هویت واقعی سمت سرور (`password_hash` bcrypt، نشست، OTP، 2FA مدیر) | ✅ |
| CSRF، Rate-Limit، هاردنینگ کوکی، فشرده‌سازی زمان‌سنجی | ✅ |
| سیاست دسترسی هر جدول (کاربران/تنظیمات قفل، حذف IDOR) | ✅ |
| محافظت `config.php`، `includes/`، `storage/` (گارد + .htaccess) | ✅ |
| گزارش‌دهی خطای بدون افشای اطلاعات | ✅ |
| اسکینگ ورودی‌های کاربر در frontend (XSS) | ✅ نقاط پرخطر |

**نتایج تست در این مخزن:** `tests/engine_dialect.php` (۳۹ تست یونیت SQL) + `tests/e2e_backend.sh` (۵۳ تست سرتاسری API روی SQLite واقعی) + `tests/panels_e2e.sh` (۱۶ تست ادپتر پنل با سرور جعلی) — **مجموعاً ۱۰۸ تست، همه سبز.**

---

## ۲) آسیب‌پذیری‌های یافت‌شده و وضعیت اصلاح

### 🔴 بحرانی

**C-1) افشای کامل جدول کاربران + هش رمز عبور**
`Db.php` قدیمی در `action=fetchAll&table=users` کل رکوردها را بدون هیچ احراز هویتی برمی‌گرداند — شامل `password_hash` (که عملاً base64 ساده بود، پس رمزها عملاً قابل‌استخراج).
✅ **اصلاح:** اکشن‌های عمومی CRUD با سیاست جدولی قفل شدند؛ خواندن `users`/`settings` فقط با سشن مدیر؛ همه پاسخ‌ها از تابع `ary_strip_secrets()` عبور می‌کنند (حذف `password_hash`، `two_factor_password_hash`). تست: «guest cannot read users»، «admin_list hides password_hash».

**C-2) نوشتن/حذف آزاد روی هر جدولی برای هر کسی**
`upsert`/`delete` بدون احراز هویت: مهاجم می‌توانست قیمت محصولات را عوض کند، سفارش جعلی بسازد، کاربر تازه با `password_hash` ساخت، یا کل داده‌ها را پاک کند.
✅ **اصلاح:** `write_guard()` — هر نوشتن باید یا با **توکن Bearer API_SECRET** باشد یا **same-origin + توکن CSRF معتبر**؛ نوشتن روی `products/categories/settings` فقط مدیر؛ `users` از این مسیر اصلاً نوشتن‌پذیر نیست (مسیر اختصاصی `user_*`). تست‌ها: upsert بدون CSRF، Origin بیگانه، نوشتن مهمان.

**C-3) IDOR در ویرایش/حذف کاربر**
`user_delete` تنها با `id` کار می‌کرد — هر کسی با حدس ID می‌توانست حساب دیگران را حذف کند.
✅ **اصلاح:** حذف/ویرایش کاربر فقط با **سشن همان کاربر** یا توکن API؛ `getById` سفارش/تیکت هم با سیاست «فقط با شماره موبایل خودت» (تست: «guest fetch w/o phone blocked»، «wrong-phone fetch cannot see»، «IDOR: guest user_delete blocked»).

**C-4) CORS کاملاً آزاد + اعتبارنامه**
`Access-Control-Allow-Origin: *` در کنار `Allow-Credentials: true` — هر سایت مخربی می‌توانست با نشست کاربر قربانی درخواست بزند.
✅ **اصلاح:** CORS فقط «هم‌ریشه» (و دامنه‌های دلخواه با `ALLOWED_ORIGINS`)؛ متدها/هدرها محدود؛ تست پره‌فلایت. درخواست‌های فرم‌محور هم با بررسی `Origin` دقیق (نه `str_contains`) رد می‌شوند — حفره دورزدن نسخه قبل (`origin.includes('site.com')`) بسته شد.

**C-5) `config.php` قابل‌دانلود بودن + دسترسی عمومی به اطلاعات اتصال**
در هاست‌های آپاچی‌محور، درخواست `config.php` مستقیم می‌توانست اجرا/دانلود شود (افشای رمز دیتابیس).
✅ **اصلاح:** گارد `if (!defined('ARYA_GUARD')) 403` ابتدای فایل تولیدی + `.htaccess` با `FilesMatch` (deny config، sqlite، logs) + `RedirectMatch 404` برای `includes/` و `storage/` + ساخت فایل config با دسترسی `0640` و چک `is_writable` در `system_status` برای هشدار.

### 🟠 بالا

**H-1) «هش» رمز سمت کلاینت با btoa + تأیید محلی**
ثبت‌نام/ورود در `pages.js` با `btoa(password)` و مقایسه در localStorage بود — عملاً رمزها بدون هیچ محافظتی در مرورگر ذخیره می‌شدند؛ «بازیابی رمز» بدون هیچ تأییدی رمز را عوض می‌کرد!
✅ **اصلاح کامل:** ثبت‌نام/ورود/بازیابی روی سرور رفت: `password_hash()` (bcrypt cost 11) سمت PHP، ورود ۲ مرحله‌ای (رمز → OTP) برای کاربران و ۳ مرحله‌ای برای مدیران (رمز → OTP → 2FA اختیاری)، بازیابی با کد OTP، و `password_verify` پایدار از نظر زمان. در حالت بدون بک‌اند، دمو همان رفتار قبلی با برچسب «آزمایشی» باقی مانده است.

**H-2) نداشتن CSRF برای اکشن‌های وضعیت‌محور**
✅ **اصلاح:** توکن CSRF تصادفی در سشن؛ اعتبارسنجی روی هر POST/PUT/DELETE غیر-توکنی؛ پاسخ‌های `admin_session`/`user_login_step2`/`csrf` توکن تازه می‌دهند؛ کلاینت‌ها (`Arya_api.js`، `adminApi` در admin.html، `adminApiCall` در adminP.js) خودکار توکن را می‌گیرند، می‌فرستند و روی ۴۰۳ یک‌بار refresh+retry می‌کنند.

**H-3) نداشتن Rate Limiting (بروت‌فورس رمز و OTP)**
✅ **اصلاح:** محدودساز فایل‌محور `storage/rate` (سازگار هاست مشترک): ورود مدیر ۸ تلاش/۱۰دقیقه به‌ازای هر شناسه+IP، ثبت‌نام ۸/۱۰دقیقه، OTP (مرحله ۲) ۲۵/۱۰دقیقه + سقف ۵ تلاش خطا روی همان سشن، نظرات/تیکت‌ها هم محدود؛ throttle عمومی ۹۰ درخواست/دقیقه برای همه اکشن‌های نوشتنی. تست: «throttle engaged».

**H-4) بازتولید خودکار «مدیر پیش‌فرض» با رمز ثابت در هر درخواست**
نسخه قبل هر بار که مدیر یافت نمی‌شد، `Admin@123456` را می‌ساخت — ورود همیشگی باز می‌ماند.
✅ **اصلاح:** ساخت مدیر اولیه **فقط در ویزارد نصب**؛ اگر رمز ندهید رمز تصادفی ۲۰ بایتی ساخته و **یک‌بار** نمایش داده می‌شود؛ پرچم `must_change_password=1` ست می‌شود و پنل مدیریت تا تغییر رمز، صفحه اجباری تغییر رمز را نشان می‌دهد.

**H-5) افشای کد OTP در پاسخ API**
✅ **اصلاح:** `otp_demo` فقط وقتی برگردد که `DEMO_MODE=true` باشد (پیش‌فرض ویزارد: خاموش). پاسخ خطای کد اشتباه، هیچ اطلاعاتی از صحت رمز مرحله قبل نمی‌دهد و مقایسه با `hash_equals` + هش HMAC انجام می‌شود (زمان یکنواخت). **بلوکر انتشار:** تا وصل‌شدن سرویس پیامک/ایمیل واقعی، سایت عملاً برای کاربر واقعی قابل‌استفاده نیست (بخش ۵).

**H-6) نشست‌ستیزی ضعیف**
✅ **اصلاح:** `session.use_strict_mode=1`، کوکی `HttpOnly` + `Secure` (روی HTTPS خودکار) + `SameSite=Lax`، `session_regenerate_id` بعد از هر ورود موفق، `save_path` خودترمیم (اگر /tmp قابل‌نوشت نباشد `storage/sessions` با .htaccess بسته) — روی هاست‌هایی مثل این تست هم نشست درست کار می‌کند.

**H-7) `generate-password-hash.php` عمومی**
✅ **اصلاح:** با ساخت `config.php`، نسخه وب خودقفل می‌شود (۴۰۳)؛ برای کار دستی حالت CLI اضافه شد: `php generate-password-hash.php "رمز"` + تغییر جزیی فرم پیش‌از‌نصب با `htmlspecialchars`.

### 🟡 متوسط

**M-1) خطاهای DB با جزئیات SQL به کاربر نمایش داده می‌شد** (افشای مسیر/میزبان/ساختار).
✅ پیام عمومی + ثبت در `storage/logs/api.log`؛ در `DEMO_MODE` پیام کامل فقط برای تست. `set_exception_handler` عمومی هم مانع نشت استک‌تریس است.

**M-2) `action=import` بدون سقف و بدون تراکنش** (قفل‌کردن جدول با ورودی بزرگ، نصفه‌کاره موندن داده).
✅ سقف ۵۰۰ رکورد + `beginTransaction/rollback`. تست: «import over 500 rejected».

**M-3) تزریق SQL از مسیر نام جداول/فیلدها** (نسخه قدیمی نام جدول را داخل SQL می‌ذاشت).
✅ آکرون‌لیست جداول + `quoteIdent` با sanitation کاراکترها (حروف/ارقام/_ فقط) + تمام مقادیر با prepared statement. تست‌ها: «review vote injection blocked»، رد `record.id` مخرب.

**M-4) XSS در رندر سمت کلاینت**: متن نظرات/تیکت‌ها و نام‌ها بدون اسکیپ در `innerHTML` تزریق می‌شدند (pages.js)؛ در پنل هم `value="${u.name}"` و پیام مودال حذف بدون اسکیپ بود (adminP.js).
✅ `aryEsc()` در همه نقاط نمایش متن کاربر در storefront؛ `escapeHtml` در adminP (عنوان‌ها، پیام‌های تیکت، نام مدیران، ورودی‌های attribute)؛ `escapeJs` برای آرگومان‌های `onclick` باقی‌مانده.

**M-5) SSRF از مسیر `panel_probe`/`panel_provision`** (سرور به آدرس دلخواه درخواست می‌زد).
✅ فقط پیش از نصب فعال‌اند، بعد از نصب `403` دائمی؛ فقط same-origin؛ throttle ۱۰ و ۵ بار/۱۰دقیقه؛ اعتبارنامه پنل هیچ‌جا ذخیره/لاگ نمی‌شود. (برای محیط‌های حساس: محدودکردن `host` به whitelist در صورت نیاز.)

**M-6) نبود `X-Frame-Options`/CSP و کش‌شدن پاسخ API.**
✅ هدرهای `nosniff`, `SAMEORIGIN`, `Referrer-Policy`, CSP منعطف (با یادداشت سفت‌کاری) و `Cache-Control: no-store` روی `Db.php` در `.htaccess`.

### ⚪ کم / یادداشت‌ها

- **L-1)** `/_sdk/*.js` (اسکریپت‌های محیط پیش‌نمایش) روی هاست واقعی ۴۰۴ می‌دهند — بی‌ضررند ولی می‌توانید حذفشان کنید.
- **L-2)** کوکی سشن روی HTTP بدون TLS ارسال می‌شود → HTTPS اجباری کنید (بلوک آماده در `.htaccess` کامنت‌دار است).
- **L-3)** نرخ‌محدودساز مبتنی بر IP پشت CDN نیازمند `TRUST_PROXY_HEADERS=true` است (وگرنه همه یک IP می‌بینید و throttle زود فعال می‌شود).
- **L-4)** `API_SECRET` نمونه در config.sample را حتماً عوض کنید؛ مقدار `CHANGE_ME*` در سرور معتبر شمرده نمی‌شود.

---

## ۳) معماری جدید (در یک نگاه)

```
Db.php                 ← روتر تک‌نقطه‌ای؛ سیاست‌ها؛ پاسخ‌های JSON یکدست
includes/security.php  ← کوکی/CSRF/throttle/رمزنگاری/اعتبارسنجی/لاگ
includes/db_engine.php ← AryaDbEngine: رجیستری درایورها + مترجم SQL/DDL (mysql|pgsql|sqlite|sqlsrv)
includes/panels.php    ← AryaHttp + ادپترهای cPanel(UAPI)/DirectAdmin(API2)/Plesk(XML) + حالت manual
config.php             ← فقط با ویزارد یا از روی config.sample.php؛ محافظت‌شده
storage/{rate,logs,sessions}/ + .htaccess deny
tests/                 ← engine_dialect.php | e2e_backend.sh | panels_e2e.sh + fake_panels.php
```

- **دیتابیس:** طرح منطقی یک‌بار تعریف می‌شود و به گویش هر موتور ترجمه می‌شود (DDL، UPSERT با `ON DUPLICATE KEY / ON CONFLICT / MERGE`، `NOW` هر موتور، فهرست ستون‌ها از اطلاعات اختصاصی هر درایور). افزونه درایور تازه = چند تابع در `SUPPORTED`.
- **پنل:** ویزارد می‌تواند دیتابیس و کاربر دیتابیس را مستقیماً در cPanel/DirectAdmin/Plesk بسازد (پیشوند خودکار cPanel `user_` رعایت می‌شود؛ DA با `DB_CREATE` یکپارچه؛ Plesk با packet API و شناسایی site-id از دامنه). اعتبارنامه فقط در همان درخواست استفاده و دور ریخته می‌شود.
- **فرانت‌اند:** `Arya_api.js` (کلاینت رسمی) → در حالت پیکربندی، احراز هویت/نظر/تیکت/سفارش سمت سرور؛ در نبود سرور، دمای محلی IndexedDB (با برچسب آزمایشی) ادامه می‌دهد. IndexedDB کش محلی برای کاتالوگ باقی می‌ماند.

---

## ۴) چطور تست کنیم؟

```bash
# ۱) تست یونیت دایالکت‌ها
php tests/engine_dialect.php

# ۲) تست سرتاسری API (روی php -S جدا؛ config قبلی را بردارید)
php -S 127.0.0.1:8211 -t . &
rm -f config.php storage/arya_store.sqlite
BASE_URL=http://127.0.0.1:8211 bash tests/e2e_backend.sh

# ۳) تست پنل‌ها (سرور جعلی + نصب موقت بدون config)
PHP_BIN=php bash tests/panels_e2e.sh
```

---

## ۵) چک‌لیست پیش از انتشار عمومی (بلاکر)

1. **وصل کردن سرویس OTP واقعی** (پیامک/ایمیل SMTP) — تا هنوز، `DEMO_MODE=true` یعنی «هر کسی که رمز درست را می‌داند با کد نمایشی وارد می‌شود». نقطه اتصال در `Db.php` تابع `otpResponse()` است — همین‌جا باید سرویس پیامک/ایمیل صدا زده شود.
2. `DEMO_MODE=false` و `API_SECRET` تصادفی در `config.php`.
3. HTTPS + فعال‌کردن بلوک redirect در `.htaccess` (روی nginx: `return 301 https://...`).
4. رمز مدیر پیش‌فرض عوض شده باشد (پنل تا تغییر رمز، صفحه اجباری نشان می‌دهد).
5. `storage/` بی‌اجازه از وب و قابل‌نوشت برای PHP؛ `config.php` با `0640`.
6. بکاپ دوره‌ای دیتابیس + خارج از وب‌روت (فایل‌های `.sqlite/.sql` با deny پوشیده‌اند اما بکاپ را جای دیگری بگذارید).
7. به‌روزرسانی منظم PHP (≥ 8.1 پیشنهاد می‌شود؛ کد روی ۸.۰ هم کار می‌کند).
8. اگر از CDN استفاده می‌کنید: `TRUST_PROXY_HEADERS` فقط در صورتی که واقعاً پشت همان CDN باشید.
